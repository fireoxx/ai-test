---
name: tech-doc
description: 生成标准技术方案文档，用于需求分析阶段输出，待用户确认后实施
---

# 生成技术方案文档

输出标准格式的技术方案文档，保存到 `.windsurf/技术方案/` 目录。

## 工作流程

1. **收集信息**：阅读现有代码，确认需求概述、影响范围、接口设计、数据库设计已分析完毕
2. **按模板生成文档**：参考下方文档模板，填充对应内容
3. **保存文件**：路径 `.windsurf/技术方案/{需求名称}.md`
4. **等待确认**：告知用户文档路径，等待确认后再实施

---

## 项目结构速查

```
apps/server/
├── api/v1/           # Handler 层（接口入口，只做参数绑定/校验/调用service/返回响应）
│   ├── system/       # 系统模块
│   └── example/      # 示例模块（新增模块参照此目录结构）
├── model/            # 数据模型
│   ├── common/       # 公共模型（request/response）
│   ├── system/       # 系统模型
│   └── {module}/     # 业务模型（含 request/ 和 response/ 子目录）
├── service/          # 业务逻辑层（禁止在 handler 直接操作数据库）
├── router/           # 路由注册（按模块分文件）
├── global/           # 全局变量（GVA_DB、GVA_LOG、GVA_CONFIG、GVA_REDIS）
├── middleware/       # Gin 中间件
└── utils/            # 工具函数
```

**前端：**
```
apps/web/src/
├── view/             # 页面组件（按模块分目录）
├── api/              # 前端接口封装（对应后端路由）
└── router/           # 前端路由
apps/H5Drift/src/
├── views/            # H5 页面组件
└── api/              # H5 接口封装
```

---

## 关键规范速查

### Handler 层
- 参数绑定：`c.ShouldBindJSON` / `c.ShouldBindQuery`
- 成功响应：`response.OkWithData(data, c)` / `response.OkWithMessage(msg, c)`
- 失败响应：`response.FailWithMessage(err.Error(), c)`
- 日志：`global.GVA_LOG.Error("失败", zap.Error(err))`
- 禁止在 handler 直接写业务逻辑或操作数据库

### Service 层
- 数据库操作：`global.GVA_DB`
- 事务：`global.GVA_DB.Transaction(func(tx *gorm.DB) error { ... })`
- 日志：`global.GVA_LOG.Error/Info/Warn`

### Model 层
- 嵌入 `global.GVA_MODEL` 获取 ID/CreatedAt/UpdatedAt 字段
- 字段必须带完整 tag：`json` + `gorm` + 可选 `binding`
- 新增字段追加到结构体**最后一行**，禁止插入中间
- 请求结构体放 `model/{module}/request/`，响应结构体放 `model/{module}/response/`

### 路由层
- 路由文件放 `router/{module}/`
- 需要鉴权的路由挂在带有 `JWTAuth()` 中间件的路由组下
- 查看 `router/` 目录下现有文件确认路由分组

### 数据库规范
- **优先使用 PostgreSQL**
- **使用 MCP pgsql 工具操作数据库，不生成独立 SQL 文件**
- 表字段变更通过 GORM AutoMigrate，不手写 DDL
- 字段规范参照 pgsql 建表规范（`specification.md`）：
  - 主键：`id bigint GENERATED BY DEFAULT AS IDENTITY NOT NULL`
  - 时间：`timestamp` 类型，默认值 `1970-01-01 08:00:00`
  - 字符串：`varchar`，小数：`decimal`，禁止 float/double
  - 所有字段 `NOT NULL DEFAULT`，text/jsonb 除外
  - 禁止外键约束，禁止创建触发器

---

## 问题自我总结机制

### 触发时机
在任务实施过程中（实施阶段、调试阶段），遇到以下情况时**必须**记录：
- 技术方案与实际代码不符，需要修正
- 违反了项目规范（api/v1、service、model、router 等层约定）
- 代码逻辑错误导致需要二次修改
- 对项目已有结构理解有误
- 编译失败、接口错误等需要回溯修改的情况

### 问题记录格式
每次遇到问题，在脑内用以下结构梳理，**任务结束后写入 PG**：

```
错误类型：logic（逻辑）/ spec（规范）/ perf（性能）/ compile（编译）
错误分类：code（代码）/ spec（规范）/ tech-doc（方案）
错误描述：（简洁描述发生了什么）
根本原因：（为什么会出错）
解决方案：（如何修正）
预防措施：（下次如何避免）
关键词：（便于检索）
```

### 何时生成问题汇总文档
满足以下任一条件时，在任务结束后在 `.windsurf/问题汇总/` 目录生成汇总文档：
- 本次任务遇到问题 **≥ 3 个**
- 遇到了**规范类**错误（之前犯过类似的错）
- 用户明确要求生成

文档路径：`.windsurf/问题汇总/{需求名称}-问题汇总.md`

### 问题汇总文档模板

```markdown
# {需求名称} - 问题汇总

> 任务时间：{日期} | 问题总数：{N}

---

## 问题清单

### P1. {问题标题}
- **错误类型**：logic / spec / perf / compile
- **错误分类**：code / spec / tech-doc
- **描述**：{发生了什么}
- **根本原因**：{为什么}
- **解决方案**：{怎么修}
- **预防措施**：{下次如何避免}
- **关键词**：`关键词1`, `关键词2`

---

## 总结与学习

### 本次高频错误
{统计哪类错误出现最多}

### 需要强化的规范
{列出需要特别注意的项目规范}

### 下次改进点
{下次任务开始前要注意什么}
```

---

## 文档模板

```markdown
# {需求名称} 技术方案

> 状态：待确认 | 创建时间：{当前日期}

---

## 一、需求概述

### 功能描述
{功能描述，说明要做什么}

### 核心目标
1. {目标1}
2. {目标2}

### 特殊约束
- {约束条件，如时间限制、兼容性要求、权限控制等，没有则写"无"}

---

## 二、影响范围

### 涉及文件

**后端新增文件**
| 文件路径 | 说明 |
|---------|------|
| `apps/server/api/v1/{module}/xxx.go` | Handler 层接口实现 |
| `apps/server/service/{module}/xxx.go` | Service 业务逻辑 |
| `apps/server/model/{module}/xxx.go` | Model 数据模型 |
| `apps/server/model/{module}/request/xxx.go` | 请求结构体 |
| `apps/server/model/{module}/response/xxx.go` | 响应结构体 |
| `apps/server/router/{module}/xxx.go` | 路由注册 |

**前端新增文件**
| 文件路径 | 说明 |
|---------|------|
| `apps/web/src/view/{module}/index.vue` | 管理后台页面 |
| `apps/web/src/api/{module}.js` | 前端接口封装 |
| `apps/H5Drift/src/views/{module}.vue` | H5 页面（如涉及） |
| `apps/H5Drift/src/api/{module}.js` | H5 接口封装（如涉及） |

**改动文件**
| 文件路径 | 改动说明 |
|---------|---------|
| `apps/server/api/v1/enter.go` | 注册新 API 结构体 |
| `apps/server/service/enter.go` | 注册新 Service 结构体 |
| `apps/server/router/enter.go` | 注册新 Router |
| `apps/server/initialize/router.go` | 初始化路由组（如需新增路由组） |

### 涉及数据库表
| 表名 | 操作 | 说明 |
|-----|------|------|
| `xxx_table` | 新增 | xxx 功能表 |
| `yyy_table` | 改动 | 新增 xxx 字段 |

---

## 三、数据库设计

> **说明**：本项目使用 PostgreSQL，通过 MCP pgsql 工具操作，通过 GORM AutoMigrate 迁移表结构，不生成独立 SQL 文件。

### 3.1 新增表结构（Model 定义）

```go
type {StructName} struct {
    global.GVA_MODEL
    Name      string    `json:"name"      gorm:"column:name;not null;default:'';comment:名称"`
    Status    int       `json:"status"    gorm:"column:status;not null;default:1;comment:状态 1启用 0禁用"`
    CreatedAt time.Time `json:"createdAt" gorm:"column:created_at;not null;default:'1970-01-01 08:00:00';comment:创建时间"`
    UpdatedAt time.Time `json:"updatedAt" gorm:"column:updated_at;not null;default:'1970-01-01 08:00:00';comment:更新时间"`
}

func ({StructName}) TableName() string {
    return "{table_name}"
}
```

### 3.2 字段变更（追加到 Model 结构体末尾）

```go
// 在 model/{module}/xxx.go 结构体最后追加
FieldName string `json:"fieldName" gorm:"column:field_name;not null;default:'';comment:{字段注释}"`
```

### 3.3 AutoMigrate 注册

在 `initialize/gorm.go`（或对应的迁移入口）中注册新表：
```go
db.AutoMigrate(&model.{StructName}{})
```

---

## 四、接口设计

### 4.1 新增接口

#### {接口功能名}
- **路由**：`{HTTP方法} /api/{version}/{资源}/{动作}`
- **路由分组**：需要鉴权（带 `JWTAuth()` 中间件）/ 公开接口
- **功能描述**：{描述}

**Swagger 注解**
```go
// @Tags     {ModuleName}
// @Summary  {接口描述}
// @Produce  application/json
// @Param    data  body      {module}Req.{XxxRequest}  true  "{请求体描述}"
// @Success  200   {object}  response.Response{data={module}Res.{XxxResponse},msg=string}  "成功"
// @Router   /api/{path} [{method}]
```

**请求参数**
| 字段 | 类型 | 必填 | 说明 |
|-----|------|------|------|
| `field_name` | string | 是 | 字段说明 |
| `page` | int | 否 | 页码，默认1 |
| `pageSize` | int | 否 | 每页数量，默认20 |

**返回示例**
```json
{
  "code": 0,
  "msg": "ok",
  "data": {
    "list": [],
    "total": 0,
    "page": 1,
    "pageSize": 20
  }
}
```

### 4.2 改动接口

#### {接口名}（原路由：`{路由}`）
| 改动类型 | 字段/参数 | 改动说明 |
|---------|---------|---------|
| 新增入参 | `xxx` | 说明 |
| 新增返回字段 | `yyy` | 说明 |

### 4.3 删除接口

| 路由 | 方法 | 说明 |
|-----|------|------|
| {如无则填：无} | - | - |

---

## 五、核心实现思路

### 5.1 Handler 层
```go
func (a *XxxApi) CreateXxx(c *gin.Context) {
    var req xxxReq.CreateXxxRequest
    if err := c.ShouldBindJSON(&req); err != nil {
        response.FailWithMessage(err.Error(), c)
        return
    }
    if err := xxxService.CreateXxx(req); err != nil {
        global.GVA_LOG.Error("创建失败", zap.Error(err))
        response.FailWithMessage("创建失败", c)
        return
    }
    response.OkWithMessage("创建成功", c)
}
```

### 5.2 Service 层
{业务逻辑说明，数据流描述，关键实现点}

### 5.3 {其他功能点}
{实现逻辑说明}

### 5.4 关键注意事项
- {注意事项1}
- {注意事项2}

---

## 六、风险点

| 风险 | 影响 | 应对措施 |
|-----|------|---------|
| {风险描述} | {影响描述} | {应对措施} |

---

## 七、实施顺序建议

1. 通过 MCP pgsql 确认数据库表结构
2. 新增/更新 Model 定义（`model/{module}/xxx.go`）
3. 注册 AutoMigrate（`initialize/gorm.go`）
4. 新增请求/响应结构体（`model/{module}/request/` 和 `response/`）
5. 实现 Service 业务逻辑（`service/{module}/xxx.go`）
6. 实现 Handler 层（`api/v1/{module}/xxx.go`）
7. 注册路由（`router/{module}/xxx.go`）
8. 在各 `enter.go` 中注册新增的结构体
9. 前端接口封装 + 页面开发（如涉及）
10. 联调验证
```

---

## 使用注意事项

1. **不存在的内容不要捏造**：接口字段、数据库字段必须来自代码或用户描述，不得猜测
2. **影响范围要完整**：不要遗漏 `enter.go` 等注册文件的改动
3. **数据库操作通过 MCP pgsql**：不生成独立 SQL 文件，不手写 DDL
4. **路由分组要准确**：查看 `router/` 目录下现有文件及 `initialize/router.go` 确认路由组定义
5. **文档输出后不实施**：生成文档是 /plan 阶段的终点，等用户确认
6. **方案中的不确定点必须列出**：需要用户决策的点逐一确认后再实施，不可自行假设
